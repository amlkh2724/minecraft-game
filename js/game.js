import { createBoard as createBoardGame } from "./board.js";

function createBoard() {
    createBoardGame();
  }
  createBoard()
  
//   function gameLoop() {
//     startGame();
//     requestAnimationFrame(gameLoop);
//   }
  
//   requestAnimationFrame(gameLoop);
  